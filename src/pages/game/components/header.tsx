import { FC } from "react";

const GameHeader: FC = () => {
    return (
        <div className="relative w-full h-[8vh] bg-[#EBE6D3] z-50 flex items-center gap-4"></div>
    );
};

GameHeader.displayName = "GameHeader";
export default GameHeader;
